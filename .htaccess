<IfModule mod_rewrite.c>
    RewriteEngine On

    # Cegah loop/rekursi dengan menambahkan / pada pengecekan public
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>